# Clippings NotebookLM

[English](#english) · [한국어](#한국어)

---

## English

An Obsidian plugin that analyzes Clippings notes with NotebookLM AI and auto-generates a summary and PDF presentation using NotebookLM's native slide engine.

### Features

- **Right sidebar** with one-click PDF generation and job history
- Three report modes:
  - 📋 **Detailed** — for subject-matter experts, 8–12 slides
  - ⚡ **Executive** — for C-level leaders, 5–8 slides
  - 🌱 **Easy** — for mixed / non-expert audiences, 5–8 slides
- AI summary (NotebookLM auto-generated overview) inserted as a callout block at the top of the note
- PDF generated by NotebookLM Studio (professional design, native quality)
- Real-time progress log with elapsed timer in the sidebar

### Prerequisites

**1. Install the Obsidian Web Clipper browser extension**

Install the [Obsidian Web Clipper](https://obsidian.md/clipper) extension for your browser (Chrome, Firefox, Safari, or Edge). This extension lets you save web pages directly into your Obsidian vault as Clippings notes.

> [!TIP]
> After installing, configure the extension to save clips into your `Clippings` folder.

**2. Install notebooklm-mcp-cli**

```bash
uv tool install notebooklm-mcp-cli
```

> [!TIP]
> If you don't have `uv`, install it first: https://docs.astral.sh/uv/getting-started/installation/

**3. Log in to NotebookLM**

```bash
nlm login
```

A browser window will open for Google OAuth sign-in.

### Installation

#### Option A — BRAT (recommended, auto-updates)

1. Install the [BRAT](https://github.com/TfTHacker/obsidian42-brat) community plugin.
2. Open **Settings → BRAT → Add Beta Plugin**.
3. Enter the repository URL:
   ```
   https://github.com/wikilog/obsidian-clippings-notebooklm
   ```
4. Click **Add Plugin**, then enable **Clippings NotebookLM** in Community Plugins.

#### Option B — Manual

1. Download `main.js`, `manifest.json`, and `styles.css` from the [latest release](https://github.com/wikilog/obsidian-clippings-notebooklm/releases/latest).
2. Copy them into `<vault>/.obsidian/plugins/clippings-notebooklm/`.
3. Enable **Clippings NotebookLM** in **Settings → Community Plugins**.

### Setup

1. Go to **Settings → Clippings NotebookLM**.
2. Click **🌐 Login via Browser** and complete Google sign-in.
3. Click **Check Status** to confirm login.

### Usage

1. Use the **Obsidian Web Clipper** extension to save a web page into your Clippings folder.
2. Open the saved Markdown file inside your Clippings folder.
3. Click the **NotebookLM icon** in the left ribbon to open the sidebar.
4. Click **"NotebookLM으로 요약하기"** and select a report mode.
5. AI generates a summary and PDF file automatically (progress shown in the sidebar).

**Output example:**

```markdown
> [!summary] ⚡ 핵심 리포트
> Key insight: ...
>
> 📎 **PDF:** [[Clippings/PDF/Article Title.pdf]]

# Article Title
(original content...)
```

### How It Works

The plugin uses NotebookLM's native slide engine instead of manually generating slides:

1. Creates a temporary NotebookLM notebook
2. Uploads the note content as a source (URL → PDF export → plain text, in order)
3. Fetches the AI-generated notebook overview via `nlm notebook describe`
4. Calls `nlm slides create` with mode-specific format, length, and focus prompt
5. Polls for completion (every 5 min, up to 20 min)
6. Downloads the result as a PDF file
7. Saves the file and inserts the summary + link into the note
8. Deletes the temporary notebook

### Troubleshooting

**`nlm` not found (PATH issue)**

Obsidian launched from Finder/Spotlight does not inherit your terminal `$PATH`. If the plugin cannot find `nlm`, enter the full path in **Settings → nlm CLI path**:

```
/Users/yourname/.local/bin/nlm
```

To find the path, run this in your terminal:

```bash
which nlm
```

**Login button does nothing**

Run `nlm login` directly in your terminal, then click **Check Status** in the plugin settings.

### Settings

| Setting | Default | Description |
|---------|---------|-------------|
| nlm CLI path | `nlm` | Path to the `nlm` executable |
| Clippings folder | `Clippings` | Folder where web clips are stored |
| PDF output subfolder | `PDF` | Subfolder inside Clippings for generated PDF files |
| PDF export temp folder | `exportPDF` | Temp folder for Obsidian PDF export (auto-deleted after upload) |

### Development

```bash
npm install
npm run dev               # Watch mode (auto-rebuild)
npm run build             # Production build
npm run deploy            # Build + copy to vault (uses OBSIDIAN_VAULT env var)
```

**Releasing a new version:**

```bash
# 1. Update version in manifest.json, package.json, and versions.json
# 2. Commit and push
git tag 0.8.1
git push && git push origin 0.8.1
# GitHub Actions will build and publish the release automatically
# (if a release already exists for the tag, assets are updated in place)
```

### Tech Stack

- TypeScript + esbuild
- [notebooklm-mcp-cli](https://github.com/nicholasgasior/notebooklm-mcp) — NotebookLM integration (slides, describe, download)

### License

MIT

### Star History

[![Star History Chart](https://api.star-history.com/svg?repos=wikilog/obsidian-clippings-notebooklm&type=Date)](https://star-history.com/#wikilog/obsidian-clippings-notebooklm&Date)

---

## 한국어

Obsidian 플러그인 — Clippings 폴더의 마크다운 파일을 NotebookLM AI로 분석하여 요약 + PDF 프레젠테이션을 자동 생성합니다. NotebookLM의 네이티브 슬라이드 엔진을 사용합니다.

### 기능

- **오른쪽 사이드바**에서 원클릭 PDF 생성 및 작업 내역 확인
- 3가지 리포트 모드:
  - 📋 **자세한 리포트** — 전문가 대상, 8–12 슬라이드
  - ⚡ **핵심 리포트** — C-레벨 임원 대상, 5–8 슬라이드
  - 🌱 **쉬운 리포트** — 비전문가 혼합 청중 대상, 5–8 슬라이드
- NotebookLM 자동 생성 개요를 AI 요약으로 노트 상단 callout 블록에 삽입
- NotebookLM Studio로 생성된 PDF (프로페셔널 디자인, 네이티브 품질)
- 사이드바에서 실시간 진행 로그 및 경과 시간 표시

### 전제 조건

**1. Obsidian Web Clipper 브라우저 확장 프로그램 설치**

[Obsidian Web Clipper](https://obsidian.md/clipper) 확장 프로그램을 브라우저(Chrome, Firefox, Safari, Edge)에 설치하세요. 이 확장 프로그램으로 웹 페이지를 Obsidian vault에 Clippings 노트로 바로 저장할 수 있습니다.

> [!TIP]
> 확장 프로그램 설치 후, 클리핑이 `Clippings` 폴더에 저장되도록 설정하세요.

**2. notebooklm-mcp-cli 설치**

```bash
uv tool install notebooklm-mcp-cli
```

> [!TIP]
> `uv`가 없다면 먼저 설치하세요: https://docs.astral.sh/uv/getting-started/installation/

**3. NotebookLM 로그인**

```bash
nlm login
```

브라우저에서 Google 계정으로 로그인하면 인증이 완료됩니다.

### 설치

#### 방법 A — BRAT (권장, 자동 업데이트)

1. [BRAT](https://github.com/TfTHacker/obsidian42-brat) 커뮤니티 플러그인을 설치합니다.
2. **설정 → BRAT → Add Beta Plugin** 을 엽니다.
3. 저장소 URL을 입력합니다:
   ```
   https://github.com/wikilog/obsidian-clippings-notebooklm
   ```
4. **Add Plugin** 클릭 → Community Plugins에서 **Clippings NotebookLM** 활성화.

#### 방법 B — 수동 설치

1. [최신 릴리즈](https://github.com/wikilog/obsidian-clippings-notebooklm/releases/latest)에서 `main.js`, `manifest.json`, `styles.css`를 다운로드합니다.
2. `<vault>/.obsidian/plugins/clippings-notebooklm/` 폴더에 파일을 복사합니다.
3. **설정 → Community Plugins** 에서 **Clippings NotebookLM** 을 활성화합니다.

### 초기 설정

1. **설정 → Clippings NotebookLM** 으로 이동합니다.
2. **🌐 브라우저로 로그인** 버튼을 클릭하고 Google 로그인을 완료합니다.
3. **상태 확인** 버튼을 눌러 로그인 상태를 확인합니다.

### 사용법

1. **Obsidian Web Clipper** 브라우저 확장 프로그램으로 웹 페이지를 Clippings 폴더에 저장합니다.
2. Clippings 폴더 안의 마크다운 파일을 엽니다.
3. 왼쪽 리본의 **NotebookLM 아이콘**을 클릭해 사이드바를 엽니다.
4. **"NotebookLM으로 요약하기"** 버튼을 클릭하고 리포트 모드를 선택합니다.
5. AI가 요약과 PDF 파일을 자동 생성합니다 (사이드바에서 진행 상황 확인 가능).

**결과물 예시:**

```markdown
> [!summary] ⚡ 핵심 리포트
> 핵심 인사이트: ...
>
> 📎 **PDF:** [[Clippings/PDF/아티클 제목.pdf]]

# 아티클 제목
(원본 내용...)
```

### 작동 방식

플러그인은 직접 슬라이드를 조립하는 대신 NotebookLM의 네이티브 슬라이드 엔진을 사용합니다:

1. 임시 NotebookLM 노트북 생성
2. 노트 내용을 소스로 업로드 (URL → PDF 내보내기 → 텍스트 순으로 시도)
3. `nlm notebook describe`로 NotebookLM 자동 생성 개요 가져오기
4. 모드별 형식·길이·포커스 프롬프트로 `nlm slides create` 호출
5. 완료 대기 (5분 간격 폴링, 최대 20분)
6. 결과물을 PDF 파일로 다운로드
7. 파일 저장 및 노트에 요약 + 링크 삽입
8. 임시 노트북 삭제

### 문제 해결

**`nlm`을 찾을 수 없음 (PATH 문제)**

Finder나 Spotlight로 실행한 Obsidian은 터미널의 `$PATH`를 상속받지 않습니다. `nlm`을 찾지 못할 경우 **설정 → nlm CLI 경로** 에 전체 경로를 직접 입력하세요:

```
/Users/yourname/.local/bin/nlm
```

전체 경로는 터미널에서 다음 명령으로 확인할 수 있습니다:

```bash
which nlm
```

**로그인 버튼이 작동하지 않을 때**

터미널에서 `nlm login`을 직접 실행한 뒤, 플러그인 설정에서 **상태 확인** 버튼을 누르세요.

### 설정 옵션

| 설정 | 기본값 | 설명 |
|------|--------|------|
| nlm CLI 경로 | `nlm` | nlm 실행 파일 경로 |
| Clippings 폴더 | `Clippings` | 웹 클리핑이 저장되는 폴더 |
| PDF 저장 하위 폴더 | `PDF` | 생성된 PDF가 저장될 하위 폴더 |
| PDF 내보내기 임시 폴더 | `exportPDF` | Obsidian PDF 내보내기용 임시 폴더 (업로드 후 자동 삭제) |

### 개발

```bash
npm install
npm run dev               # Watch 모드 (자동 재빌드)
npm run build             # 프로덕션 빌드
npm run deploy            # 빌드 + vault 자동 복사 (OBSIDIAN_VAULT 환경변수 사용)
```

**새 버전 릴리즈:**

```bash
# 1. manifest.json, package.json, versions.json의 버전 번호를 업데이트
# 2. 커밋 후 푸시
git tag 0.8.1
git push && git push origin 0.8.1
# GitHub Actions가 자동으로 빌드하고 릴리즈를 생성합니다
# (태그에 릴리즈가 이미 있으면 에셋만 업데이트됩니다)
```

### 기술 스택

- TypeScript + esbuild
- [notebooklm-mcp-cli](https://github.com/nicholasgasior/notebooklm-mcp) — NotebookLM 연동 (슬라이드 생성, 개요 조회, 다운로드)

### 라이선스

MIT

### Star History

[![Star History Chart](https://api.star-history.com/svg?repos=wikilog/obsidian-clippings-notebooklm&type=Date)](https://star-history.com/#wikilog/obsidian-clippings-notebooklm&Date)
